# Wulftek Website

Client website for **Wulftek** (vehicle remapping / tuning), built by Talon Insights.

## Status

New project area — no site code yet. See `..\WULFTEK PROMPT.txt` for the client's
initial brief notes:

- The vehicle **reg lookup should be the unrivalled centre of attention** on the
  home page, with a suitable background image behind it (stock/Google images as
  placeholders for now).
- The engine-software diagram is too technical for a generic consumer — drop it.
- Add a dedicated **"What is remapping?"** page; the home page carries a
  shortened version of it.
- Remove the "paper" element the client dislikes.

## Stack conventions (mirroring DJ-Website)

- Static site, deployed via GitHub → Vercel.
- GitHub org: `TalonInsights` (repo not created yet).
- Vercel team: `talon-insights` (project not created yet).
